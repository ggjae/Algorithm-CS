#include <stdio.h>
#include <string.h>
char a[101][101];
int alpha[26] = {0, };
char tmp;
int main(){
    int N;
    scanf("%d",&N);
    for(int i=0;i<N;i++){
        scanf("%s",&a[i]);
    }
    for(int i=0;i<N;i++){
        for(int j=0;j<strlen(a[i]);j++){
            tmp = a[i][j];
            if()
        }
    }
    return 0;
}