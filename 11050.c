#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int n,k;
    int sum = 0;
    scanf("%d",&n);
    scanf("%d",&k);
    for(int i=0;i<k;i++){
        sum = sum + n
    }
    return 0;
}