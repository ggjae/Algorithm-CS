#include <iostream>
#include <vector>
using namespace std;
int main(){
    int city;
    vector<int> length, price;
    vector<int> minprice;
    cin >> city;
    for(int i=0;i<city-1;i++){
        int tmp;
        cin >> tmp;
        length.push_back(tmp);
    }
    for(int i=0;i<city;i++){
        int tmp;
        cin >> tmp;
        price.push_back(tmp);
    }
    for(int i=0;i<city;i++){
        price
        minprice.push_back()
    }
    

    return 0;
}