#include <iostream>
#include <vector>
int main(){
    int n,k;
    int ele[100];
    cin >> n;
    cin >> k;
    for(int i=0;i<k;i++){
        int tmp;
        cin >> tmp;
        ele[i] = tmp;
    }
    return 0;
}