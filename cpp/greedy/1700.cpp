#include <iostream>
#include <vector>
int main(){
    int n,k;
    int ele[101];
    cin >> n;
    cin >> k;
    for(int i=0;i<k;i++){
        int tmp;
        cin >> tmp;
        ele[i] = tmp;
    }
    sort(tmp.begin(),tmp.end(),greater<>());
    
    return 0;
}