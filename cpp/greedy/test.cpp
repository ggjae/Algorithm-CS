#include <iostream>
#include <vector>
#include <algorithm>
int main(){
    int n;
    vector<int> d;
    for(int i=0;i<n;i++){
        int tmp;
        cin >> tmp;
        d.push_back(tmp);
    }
    sort(d.begin(),d.end());

    for(int i=0;i<n;i++){
        if(d[i] > )
    }
    return 0;
}