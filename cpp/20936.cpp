#include <iostream>
#include <vector>
using namespace std;
int main(){
    string m;
    getline(cin,m);
    char *ptr = strchr(m, '+')
    for(int i=0;i<m.size();i++){
        printf("%c",m[i]);
    }
    return 0;
}